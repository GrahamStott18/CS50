Specs:

- Prompt user to input the height
    - height must be an integer
    - height must be between 1 and 8 inclusive

- System needs to print a triangle where the height depends on wat user inputs
    - Define how many rows it should print
    - Define how many columns it should print
    - Define when to print spaces " " and when to print "#"